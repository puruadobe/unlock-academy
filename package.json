{
  "name": "unlock-academy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node build.mjs",
    "agent:setup": "node setup-agent.mjs"
  },
  "dependencies": {
    "@truefoundry/trueforge-sdk": "0.2.0"
  },
  "engines": {
    "node": ">=22"
  }
}
